import styled from 'styled-components';

export const Container = styled.button`
  display: flex;
  align-items: center;
  font-size: 16px;
  color: #999999;

  &:hover {
    cursor: pointer;
  }

  svg {
    margin-right: 7px;
  }
`;
